<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/engineer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/engineer"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ActualizaRegistroAmbiente"></activity>
        <activity android:name=".dicionario" />
        <activity android:name=".luxometros" />
        <activity android:name=".lstaPisosDesarpobados" />
        <activity android:name=".RegistroDatosAmbiente" />
        <activity android:name=".listaAmbientes" />
        <activity android:name=".MainActivity" />
        <activity android:name=".listaPisosActivity" />
        <activity android:name=".InstagramActivty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>